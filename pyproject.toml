[project]
name = "agentic-ai-system-course"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.104.1",
    "deepagents>=0.6.3",
    "langchain>=1.3.1",
    "python-dotenv>=1.2.2",
]
